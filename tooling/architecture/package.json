{
  "name": "@brawltome/architecture",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun test tests && bun run src/cli.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: brawltome
      POSTGRES_PASSWORD: brawltome
      POSTGRES_DB: brawltome
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U brawltome -d brawltome"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - postgres_data:/var/lib/postgresql/data

  migration:
    profiles: ["v3"]
    build:
      context: .
      target: migration
    environment:
      DATABASE_URL: postgres://brawltome:brawltome@postgres:5432/brawltome
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    profiles: ["v3"]
    build:
      context: .
      target: api
    environment:
      DATABASE_URL: postgres://brawltome:brawltome@postgres:5432/brawltome
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET}
      DISCORD_INTERNAL_API_SECRET: ${DISCORD_INTERNAL_API_SECRET}
      METRICS_SCRAPE_SECRET: ${METRICS_SCRAPE_SECRET}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_AUTHORIZATION: ${OTEL_EXPORTER_OTLP_AUTHORIZATION:-}
      REFRESH_TRUST_COOKIE_SECRET: ${REFRESH_TRUST_COOKIE_SECRET}
      REPLAY_BRIDGE_SECRET: ${REPLAY_BRIDGE_SECRET}
      AUTHENTICATED_REFRESH_IP_LIMIT: ${AUTHENTICATED_REFRESH_IP_LIMIT:-120}
      PORT: "3000"
      RUNTIME_SHUTDOWN_DEADLINE_MS: "60000"
      RUNTIME_CLEANUP_RESERVE_MS: "5000"
    ports:
      - "3000:3000"
    depends_on:
      migration:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "const r = await fetch('http://localhost:3000/health/ready'); if (!r.ok) process.exit(1)",
        ]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s
    stop_grace_period: 70s
    restart: unless-stopped

  discord-bot:
    profiles: ["v3"]
    build:
      context: .
      target: discord-bot
    environment:
      API_URL: http://api:3000
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET}
      DISCORD_INTERNAL_API_SECRET: ${DISCORD_INTERNAL_API_SECRET}
      METRICS_SCRAPE_SECRET: ${METRICS_SCRAPE_SECRET}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_AUTHORIZATION: ${OTEL_EXPORTER_OTLP_AUTHORIZATION:-}
      DISCORD_METRICS_PORT: "3002"
      RUNTIME_SHUTDOWN_DEADLINE_MS: "60000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "const r = await fetch('http://localhost:3002/health/ready'); if (!r.ok) process.exit(1)",
        ]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s
    stop_grace_period: 70s
    restart: unless-stopped

  operations-worker:
    profiles: ["v3"]
    build:
      context: .
      target: operations-worker
    environment:
      DATABASE_URL: postgres://brawltome:brawltome@postgres:5432/brawltome
      BRAWLHALLA_API_KEY: ${BRAWLHALLA_API_KEY}
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET}
      METRICS_SCRAPE_SECRET: ${METRICS_SCRAPE_SECRET}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_AUTHORIZATION: ${OTEL_EXPORTER_OTLP_AUTHORIZATION:-}
      AUTHENTICATED_REFRESH_IP_LIMIT: ${AUTHENTICATED_REFRESH_IP_LIMIT:-120}
      BRAWLHALLA_V1_REQUEST_LIMIT: ${BRAWLHALLA_V1_REQUEST_LIMIT:-1800}
      SOURCE_BACKGROUND_HEADROOM: ${SOURCE_BACKGROUND_HEADROOM:-30}
      SOURCE_UNAVAILABLE_RETRY_MS: ${SOURCE_UNAVAILABLE_RETRY_MS:-60000}
      HEALTH_PORT: "3001"
      RUNTIME_SHUTDOWN_DEADLINE_MS: "60000"
      RUNTIME_CLEANUP_RESERVE_MS: "5000"
    depends_on:
      migration:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "bun",
          "-e",
          "const r = await fetch('http://localhost:3001/health/ready'); if (!r.ok) process.exit(1)",
        ]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 10s
    stop_grace_period: 70s
    restart: unless-stopped

volumes:
  postgres_data:
